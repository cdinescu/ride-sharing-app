spring:
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false

embedded:
  postgresql:
    enabled: true
    dockerImage: postgres:latest
    user: postgres
    password: root
