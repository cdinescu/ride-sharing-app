spring:
  profiles:
    active: test
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false

  kafka:
    consumer:
      auto-offset-reset: earliest
test:
  topic: ride-planning-topic
