spring:
  profiles:
    active: test
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false

  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id: baeldung
test:
  topic: ride-planning-topic
